all:
  children:
    base:
      hosts:
        k8s-alpine:
          ansible_connection: local
          level: k8s
          os_version: alpine
        dev-alpine:
          ansible_connection: local
          level: dev
          os_version: alpine
